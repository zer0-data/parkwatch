services:
  backend:
    build:
      context: .
      dockerfile: ./Dockerfile.backend
    container_name: parkwatch-backend
    ports:
      - "8001:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=info
      - PYTHONPATH=/app
      - HF_TOKEN=${HF_TOKEN:-}
      - HF_MODEL=${HF_MODEL:-Qwen/Qwen2.5-3B-Instruct:cheapest}
      - HF_TIMEOUT_SECONDS=${HF_TIMEOUT_SECONDS:-8}
    volumes:
      - ./backend:/app/backend:z
      - ./scripts:/app/scripts:z
      - ./data:/app/data:z
    working_dir: /app
    command: python -m uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8000/api/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - parkwatch

  frontend:
    build:
      context: ./frontend
      dockerfile: ../Dockerfile.frontend
    container_name: parkwatch-frontend
    ports:
      - "3000:3000"
    environment:
      - BACKEND_URL=http://backend:8000
      - NEXT_PUBLIC_API_BASE_URL=http://backend:8000
      - NODE_ENV=production
    depends_on:
      backend:
        condition: service_healthy
    working_dir: /app
    command: npm run start
    networks:
      - parkwatch

  nginx:
    image: nginx:alpine
    container_name: parkwatch-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.docker.conf:/etc/nginx/nginx.conf:ro,z
      - ./ssl:/etc/nginx/ssl:ro,z
    depends_on:
      - backend
      - frontend
    networks:
      - parkwatch

networks:
  parkwatch:
    driver: bridge

volumes:
  parkwatch-data:
    driver: local
